<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<p>
  <strong>Author:</strong>
  <%= @article.author %>
</p>

<%#= link_to 'Edit', edit_article_path(@article) %> 
<%= link_to 'Back', articles_path %>

<h1>New comment</h1>
 <% if @comment.parent %>
  <%= render 'comments/comment', comment: @comment.parent, from_reply_form: true %>
<% end %>
<%= render 'form' %>

<%= render partial: 'comments/comment', collection: @comments %>
<%#= render 'comments/comment' %>

